// Questo file è stato sostituito da instrumentation-client.ts (convenzione corrente di
// Next.js/Sentry, compatibile con Turbopack — vedi quel file per l'inizializzazione
// reale). Lasciato vuoto invece che cancellato: l'ambiente di sviluppo di questa sessione
// non permette di eliminare file già sincronizzati nella cartella reale dell'utente.
// Nessun codice qui: non deve inizializzare Sentry una seconda volta.
export {};
